# FreshRSS connection (Google Reader API)
FRESHRSS_URL=http://localhost:8080
FRESHRSS_USER=admin
FRESHRSS_PASS=changeme

# RSS-Bridge base URL (used to generate Facebook feed URLs)
RSS_BRIDGE_URL=http://localhost:3000
